machine:
  services:
    - docker

dependencies:
  override:
    - docker info

test:
  override:
    - docker build -t gotfix/redis .
    - docker run -d --name=redis -v /run/redis gotfix/redis; sleep 10
    - docker run --volumes-from=redis gotfix/redis redis-cli -s /var/run/redis/redis.sock PING
